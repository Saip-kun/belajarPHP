<?php

// OPERATOR ARITMATIKA
    // + , - , * , / , % , **

    // $a = 10;
    // $b = 20;

    // $total = $a * $b ;

    // echo $total;

//OPERATOR ASSIGNMENT

    // $a =  20;
    // $b = 30;
    // $total = 10

    // $total = $total + $a; cara 1
    // $total = $total *= $a; cara 2

    // echo $total;

// OPERATOR PEMBANDING

    // $a = 7
    // $b = 7

    // $komparasi = $a == $b; hanya mengecek isi sama atau tidak
    // $komparasi = $a === $b; mengecek kesulurahn data bahkan ke tipe data
    // $komparasi = $a != $b; hanya mengecek isi sama atau tidak
    // $komparasi = $a !== $b; mengecek kesulurahn data bahkan ke tipe data

    // echo var_dump($komparasi);

    // UNTUK  SELENGKAPNYA BACA DI PHP OPERATOR W3SCHOOL

// OPERATOR INCREMENT/DECREMENT

    // $a = 10;
    // $a++; output tambah 1 setelah angka
    // $a--; output kurang 1 setelah angka

    // echo $a; perintah

// OPERATOR STRING

    $a = "Hello";
    $b = " World";

    echo $a . $b;

// echo (merupakan perintah output, jika di c++ seperti cout)